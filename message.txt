-- Bảng 3fix v5 - Bay tới coin gần nhất, không đổi hướng lung tung, reset khi balo đầy
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

if LocalPlayer.PlayerGui:FindFirstChild("FarmGUI3") then
    LocalPlayer.PlayerGui.FarmGUI3:Destroy()
end

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "FarmGUI3"
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 180, 0, 120)
frame.Position = UDim2.new(0, 400, 0, 250)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.BorderSizePixel = 0
frame.Active = true
frame.Parent = gui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 20)
title.BackgroundTransparency = 1
title.Text = "Bảng 3fix v5"
title.TextColor3 = Color3.fromRGB(255, 255, 0)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 18
title.Parent = frame

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(1, -20, 0, 40)
toggleBtn.Position = UDim2.new(0, 10, 0, 30)
toggleBtn.BackgroundColor3 = Color3.fromRGB(80, 160, 80)
toggleBtn.Text = "Start Farm"
toggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
toggleBtn.Font = Enum.Font.SourceSansBold
toggleBtn.TextSize = 18
toggleBtn.Parent = frame

local status = Instance.new("TextLabel")
status.Size = UDim2.new(1, -20, 0, 30)
status.Position = UDim2.new(0, 10, 0, 80)
status.BackgroundTransparency = 1
status.Text = "Status: OFF"
status.TextColor3 = Color3.fromRGB(255,100,100)
status.Font = Enum.Font.SourceSans
status.TextSize = 16
status.Parent = frame

-- Farm logic
local farming = false

local function getCoins()
    for _, map in pairs(workspace:GetChildren()) do
        if map:FindFirstChild("CoinContainer") then
            return map.CoinContainer
        end
    end
end

local function findNearestCoin()
    local character = LocalPlayer.Character
    if not character then return nil end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local container = getCoins()
    if not container then return nil end

    local nearest, dist = nil, math.huge
    for _, coin in pairs(container:GetChildren()) do
        if coin:IsA("BasePart") and coin.Parent and coin:IsDescendantOf(workspace) and coin.Transparency < 1 then
            local d = (hrp.Position - coin.Position).Magnitude
            if d < dist then
                dist = d
                nearest = coin
            end
        end
    end
    return nearest
end

local function moveToCoin(coin)
    local character = LocalPlayer.Character
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp or not coin or not coin.Parent then return end

    local distance = (hrp.Position - coin.Position).Magnitude
    local speed = 20
    local time = math.clamp(distance / speed, 0.3, 3)

    local tween = TweenService:Create(hrp, TweenInfo.new(time, Enum.EasingStyle.Linear), {CFrame = coin.CFrame + Vector3.new(0,3,0)})
    local finished = false
    tween.Completed:Connect(function()
        finished = true
        if coin and coin.Parent then
            coin:Destroy()
        end
    end)
    tween:Play()

    repeat task.wait() until finished
end

-- Kiểm tra balo đã đầy chưa bằng cách xem coin trên map còn hiển thị không
local function isBagFull()
    local container = getCoins()
    if not container then return false end
    local visibleCoinsCount = 0
    for _, coin in pairs(container:GetChildren()) do
        if coin:IsA("BasePart") and coin.Parent and coin:IsDescendantOf(workspace) and coin.Transparency < 1 then
            visibleCoinsCount = visibleCoinsCount + 1
        end
    end
    return visibleCoinsCount == 0
end

local function autoFarm()
    while farming do
        if isBagFull() then
            status.Text = "Status: FULL - Resetting..."
            status.TextColor3 = Color3.fromRGB(255, 150, 0)
            -- Reset nhân vật
            if LocalPlayer.Character then
                LocalPlayer.Character:BreakJoints()
            end
            task.wait(5) -- Đợi respawn
            status.Text = "Status: ON"
            status.TextColor3 = Color3.fromRGB(100,255,100)
        else
            local coin = findNearestCoin()
            if coin then
                moveToCoin(coin)
            else
                task.wait(0.1)
            end
        end
    end
end

toggleBtn.MouseButton1Click:Connect(function()
    farming = not farming
    if farming then
        toggleBtn.Text = "Stop Farm"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
        status.Text = "Status: ON"
        status.TextColor3 = Color3.fromRGB(100,255,100)
        task.spawn(autoFarm)
    else
        toggleBtn.Text = "Start Farm"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(80,160,80)
        status.Text = "Status: OFF"
        status.TextColor3 = Color3.fromRGB(255,100,100)
    end
end)
